<% if record.images.attached? %>
  <% record.images.each do |image| %>
    <%= image_tag(image.variant(resize: '50x50')) %>
    <%= link_to 'Remove', image_path(image.id), remote: true, method: :delete, data: { confirm: 'Are you sure?' } %>
  <% end %>
<% end %>
