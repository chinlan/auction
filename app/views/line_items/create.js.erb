<% if @line_item.errors.any? || @cart.errors.any? %>
  alert("<%= escape_javascript(@line_item.errors.full_messages.first || @cart.errors.full_messages.first) %>");
<% else %>
  $(".cart-text").html("<%= escape_javascript(render 'layouts/cart_text') %>");
<% end %>
